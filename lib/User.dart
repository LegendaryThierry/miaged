class User{
  String id;
  String login;
  String password;
  String birthday;
  String address;
  String postalCode;
  String city;


  User(this.id, this.login, this.password, this.birthday, this.address, this.postalCode, this.city);
}